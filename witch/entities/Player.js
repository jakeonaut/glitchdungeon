//Inheritance
Player.prototype = Object.create(GameMover.prototype);

function Player(x, y, image){
	GameMover.call(this, x, y, 2, 2, 14, 14, image, 162);
	
	/** FUNCTION DEFINITIONS****************************************/
	/**????????????????????????????????????????????????????????????*/
	Player.prototype.Update = function(delta, map){
		// this.UpdateAnimation()
		GameMover.prototype.Update.call(this, delta, map);
	}
}